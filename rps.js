function getComputerChoice(){
    const compChoice = Math.floor(Math.random() * 3 + 1);
    console.log(compChoice);
    return compChoice;
}

function getHumanChoice(){
    const userChoice = prompt('1, 2 or 3 for rock, paper or scissors?');
    console.log(userChoice);
    return userChoice;
}

function calculateWinLoss(user, computer){
    if (computer === 1 && user === )
}
getComputerChoice();
getHumanChoice();